overwrite: true
schema: './graphql/schema.graphql'
documents: null
generates:
  graphql/generated/graphql.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
hooks:
  afterOneFileWrite:
    - prettier --write
