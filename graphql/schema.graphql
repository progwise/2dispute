# import Subject, SubjectCreateInput from './Subject/Subject.graphql'

type Query {
  allSubjects: [Subject!]!
  subject(id: ID!): Subject
}

type Mutation {
  createSubject(input: SubjectCreateInput!): Subject!
}
