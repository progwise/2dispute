# import Message from '../Message/Message.graphql'

type Dispute {
  id: ID!
  subject: Subject!
  partnerA: User!
  partnerB: User!
  messages: [Message!]!
  createdAt: DateTime!
}

input ReplyOnDisputInput {
  disputeId: ID!
  message: String!
}
