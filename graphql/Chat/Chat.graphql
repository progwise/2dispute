interface ChatItem {
  id: ID!
  lastUpdateAt: DateTime!
}

type Chat {
  items: [ChatItem!]!
  newestLastUpdateAt: DateTime
  oldestLastUpdateAt: DateTime
  hasNextPage: Boolean!
}

enum ChatScope {
  ALL
  USER_SCOPE
}
